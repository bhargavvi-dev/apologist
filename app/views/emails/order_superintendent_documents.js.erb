$('.modal').modal('hide');
<% if @commission.buying? or @commission.renting_tenant? %>
  setTimeout(function(){
    $("#visitReportStep_" + "<%= params[:section_name].to_s.downcase%>" + "Modal"+"<%= @visit_report.id%>").modal('show');
  }, 1000);
<% else %>
  setTimeout(function(){
    $('#OrderSuperintendentDocumentsSuccessModal').modal('show');
  }, 500);
<% end %>
// $('html,body').animate({scrollTop: 0 }, 'slow');
// setFlashMessage("set_flash_msg", "set_flash_msg_notice", "<%= t('message.email_successfully_sent') %>");
<%= render :partial => "shared/ajax_js" %>