.container
  .formField
    .row
      #registration-form.col-xs-12
        = render "form_step_#{@step.to_s}"
  - if @step.to_i == 2
    #openingHours.modal.fade{"aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
      .modal-dialog.modal-lg{:role => "document"}
        .modal-content
          .modal-body
            = render 'opening_hours'