.nested-fields
  .form-group.fieldRemove
    = f.text_field :name, :class => "form-control"
    = link_to_remove_association f, :class => 'addLink' do
      %i.removeIcon
      = t('office.remove_team')